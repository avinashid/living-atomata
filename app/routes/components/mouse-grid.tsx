import { useMousePosition } from "~/hooks/useMousePosition";
import { useEffect } from "react";
import { useMouseState } from "../state/mouse-state";

export const MouseEffect = (props?: { className?: string }) => {
  // const rawMLocation = useMousePosition();
  // const mouse = useMouseState((e) => e.updateMouseLocation);

  // useEffect(() => {
  //   if (rawMLocation?.x && rawMLocation?.y) {
  //     mouse(rawMLocation);
  //   }
  // }, [rawMLocation]);
  return <></>;
};
